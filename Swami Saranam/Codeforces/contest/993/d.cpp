#include <bits/stdc++.h>
using namespace std;
#define ll long long 

int mode=
int main(){
    int number;
    cin>>number;
    for(int i=0;i<number;i++){
        int inputs;
        cin>>inputs;
        vector<int>nums1(inputs);
        for(int p=0;p<inputs;p++){
            cin>>nums1[p];
        }
        vector<int>ans;
        ans[0]=nums1[0];
        for(int p=1;p<nums1.size();p++){
            if(mode(ans1)==nums1[p]){

            }
            else{

            }
        }
    }
}